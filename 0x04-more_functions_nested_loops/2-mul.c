#include "main.h"
#include <stdio.h>

/**
 * mul - Takes an integer and returns whether it is 0 - 9.
 *
 * @a: The first integer to be multiplied
 * @b: The second integer to be multiplied
 *
 * Return: 1 if upper. 0 if otherwise.
 **/

int mul(int a, int b);

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
