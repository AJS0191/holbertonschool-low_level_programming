#include "main.h"
/**
 * add - returns the sum
 * @int: takes 2 integers both integers are to be added
 *
 * Description: adds both intergers together and returns the sum
 *
 *
 * Return: returns results of addition
 */
int add(int, int);

int add(int a, int b)
{
	int sum = (a + b);

	return (sum);
}
